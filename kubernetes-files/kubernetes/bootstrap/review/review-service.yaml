apiVersion: v1
kind: Service
metadata:
  name: review
spec:
  selector:
    app: review
  type: NodePort             # expose service outside cluster
  ports:
    - port: 80               # service port
      targetPort: 8084       # container port
      nodePort: 30082        # fixed host port