apiVersion: v1
kind: Service
metadata:
  name: company
spec:
  selector:
    app: company
  type: NodePort           # works reliably in Minikube
  ports:
    - port: 80             # service port
      targetPort: 8083     # container port
      nodePort: 30081      # fixed host port
