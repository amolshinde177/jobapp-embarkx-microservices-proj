apiVersion: v1
kind: Service
metadata:
  name: job
spec:
  selector:
    app: job
  type: NodePort               # make service accessible outside the cluster
  ports:
    - port: 80                 # service port
      targetPort: 8082         # container port
      nodePort: 30080          # fixed port on host
